---
/**
 * CoursesPage - 共享的课程报名页面组件
 * @param locale - 语言代码 ('nl' | 'en' | 'de' | 'pl' | 'zh')
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import RegistrationForm from './RegistrationForm.vue';
import { getTranslations } from '../../lib/i18n';
import { localizedPath } from '../../lib/paths';

interface Props {
  locale: 'nl' | 'en' | 'de' | 'pl' | 'zh';
}

const { locale } = Astro.props;
const currentLocale = locale;
const t = getTranslations('courses', currentLocale);
const gt = getTranslations('global', currentLocale);

// 特色功能数据
const features = [
  { key: 'professional', emoji: '✓', color: 'accent' },
  { key: 'smallClass', emoji: '✓', color: 'cyan' },
  { key: 'flexible', emoji: '✓', color: 'secondary' },
  { key: 'environment', emoji: '✓', color: 'accent' },
];

// FAQ 数据
const faqs = [
  { key: 'q1', delay: '0s' },
  { key: 'q2', delay: '0.1s' },
  { key: 'q3', delay: '0.2s' },
  { key: 'q4', delay: '0.3s' },
];

const workshopCards = [
  {
    key: 'corporate',
    href: localizedPath('/workshops/corporate', currentLocale),
    accent: 'accent',
  },
  {
    key: 'advanced',
    href: localizedPath('/workshops/advanced', currentLocale),
    accent: 'secondary',
  },
];
---

<BaseLayout page="courses" locale={currentLocale}>
  <!-- Hero Section -->
  <section class="relative min-h-[60vh] flex items-center justify-center overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-paper via-mist/30 to-paper"></div>
      <div class="absolute top-20 left-20 w-96 h-96 bg-accent/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 right-20 w-96 h-96 bg-cyan/5 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
      <div class="animate-fade-in">
        <span class="inline-block px-4 py-2 text-sm font-medium text-accent bg-accent/10 rounded-full mb-6">
          {t.hero.tag}
        </span>
        <h1 class="text-5xl md:text-6xl font-serif font-bold text-ink mb-6">
          {t.hero.title}
        </h1>
        <p class="text-xl text-secondary max-w-3xl mx-auto mb-8">
          {t.hero.subtitle}
        </p>
        <a
          href="#registration-form"
          class="inline-flex items-center space-x-2 px-8 py-4 bg-accent hover:bg-primary text-paper hover:text-paper border-2 border-accent hover:border-primary rounded-lg font-medium text-lg transition-all duration-300 hover:scale-105 hover:shadow-xl"
        >
          <span>{t.register}</span>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Pricing Section -->
  <section class="py-24 bg-mist/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
          💰
        </span>
        <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink mb-6">
          {t.pricing.title}
        </h2>
        <div class="w-32 h-1 bg-accent/50 mx-auto mb-6"></div>
        <p class="text-lg text-secondary max-w-2xl mx-auto">
          {t.pricing.subtitle}
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Beginner -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <span class="inline-block px-3 py-1 text-xs font-medium text-accent bg-accent/10 rounded-full">
                {t.cards.beginner.tag}
              </span>
            </div>
            <h3 class="text-2xl font-serif font-bold text-ink mb-3">
              {t.cards.beginner.title}
            </h3>
            <p class="text-4xl font-bold text-accent mb-2">10€</p>
            <p class="text-sm text-secondary mb-6 flex-1">{t.pricing.beginner}</p>
            <div class="border-t border-border/20 pt-4 mt-auto">
              <div class="flex items-center text-secondary text-sm">
                <span class="mr-2">🕐</span>
                <span>{t.cards.beginner.duration}</span>
              </div>
            </div>
            <div class="mt-6">
              <a
                href={localizedPath('/courses/beginner', currentLocale)}
                class="inline-flex items-center space-x-2 text-accent font-medium text-sm border border-accent/40 rounded-lg px-4 py-2 hover:bg-accent/10 transition-colors"
              >
                <span>{t.cards.beginner.outline}</span>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>

        <!-- Intermediate -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.1s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <span class="inline-block px-3 py-1 text-xs font-medium text-cyan bg-cyan/10 rounded-full">
                {t.cards.intermediate.tag}
              </span>
            </div>
            <h3 class="text-2xl font-serif font-bold text-ink mb-3">
              {t.cards.intermediate.title}
            </h3>
            <p class="text-4xl font-bold text-cyan mb-2">12€</p>
            <p class="text-sm text-secondary mb-6 flex-1">{t.pricing.intermediate}</p>
            <div class="border-t border-border/20 pt-4 mt-auto">
              <div class="flex items-center text-secondary text-sm">
                <span class="mr-2">🕐</span>
                <span>{t.cards.intermediate.duration}</span>
              </div>
            </div>
            <div class="mt-6">
              <a
                href={localizedPath('/courses/intermediate', currentLocale)}
                class="inline-flex items-center space-x-2 text-cyan font-medium text-sm border border-cyan/40 rounded-lg px-4 py-2 hover:bg-cyan/10 transition-colors"
              >
                <span>{t.cards.intermediate.outline}</span>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>

        <!-- Advanced -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.2s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <span class="inline-block px-3 py-1 text-xs font-medium text-secondary bg-secondary/10 rounded-full">
                {t.cards.advanced.tag}
              </span>
            </div>
            <h3 class="text-2xl font-serif font-bold text-ink mb-3">
              {t.cards.advanced.title}
            </h3>
            <p class="text-4xl font-bold text-secondary mb-2">16€</p>
            <p class="text-sm text-secondary mb-6 flex-1">{t.pricing.advanced}</p>
            <div class="border-t border-border/20 pt-4 mt-auto">
              <div class="flex items-center text-secondary text-sm">
                <span class="mr-2">🕐</span>
                <span>{t.cards.advanced.duration}</span>
              </div>
            </div>
            <div class="mt-6">
              <a
                href={localizedPath('/courses/advanced', currentLocale)}
                class="inline-flex items-center space-x-2 text-secondary font-medium text-sm border border-secondary/40 rounded-lg px-4 py-2 hover:bg-secondary/10 transition-colors"
              >
                <span>{t.cards.advanced.outline}</span>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>

        <!-- Workshop -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.3s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <span class="inline-block px-3 py-1 text-xs font-medium text-accent bg-accent/10 rounded-full">
                Workshop
              </span>
            </div>
            <h3 class="text-2xl font-serif font-bold text-ink mb-3">
              {t.private}
            </h3>
            <p class="text-4xl font-bold text-accent mb-2">—</p>
            <p class="text-sm text-secondary mb-6 flex-1">{t.pricing.workshop}</p>
            <div class="border-t border-border/20 pt-4 mt-auto">
              <div class="flex items-center text-secondary text-sm">
                <span class="mr-2">📅</span>
                <span>TBA</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Workshops Row -->
  <section class="py-12">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-animate">
        <h3 class="text-3xl md:text-4xl font-serif font-bold text-ink mb-4">{t.workshopsSection.title}</h3>
        <p class="text-secondary max-w-3xl mx-auto">{t.workshopsSection.subtitle}</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {workshopCards.map((card, index) => (
          <div class="scroll-animate" style={`animation-delay: ${index * 0.1}s`}>
            <div class="h-full bg-paper rounded-2xl border border-border/30 p-8 shadow-lg flex flex-col">
              <div class="mb-4">
                <span class={`inline-block px-3 py-1 text-xs font-medium text-${card.accent} bg-${card.accent}/10 rounded-full`}>
                  {t.workshopsSection.items[card.key].tag}
                </span>
              </div>
              <h3 class="text-2xl font-serif font-bold text-ink mb-3">
                {t.workshopsSection.items[card.key].title}
              </h3>
              <p class="text-secondary mb-4 flex-1">
                {t.workshopsSection.items[card.key].desc}
              </p>
              <div class="flex items-center text-secondary text-sm mb-6">
                <span class="mr-2">🕔</span>
                <span>{t.workshopsSection.items[card.key].duration}</span>
              </div>
              <a
                href={card.href}
                class={`inline-flex items-center justify-center px-5 py-3 rounded-lg border-2 text-sm font-medium transition-all duration-300 border-${card.accent} text-${card.accent} hover:bg-${card.accent}/10`}
              >
                {t.workshopsSection.items[card.key].cta}
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Course Features -->
  <section class="py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <!-- Left image -->
        <div class="scroll-animate order-2 lg:order-1">
          <div class="relative">
            <div class="aspect-[4/3] rounded-2xl overflow-hidden shadow-2xl">
              <img
                src="/image/taichi-practice.jpg"
                alt="Tai Chi Chuan Course"
                class="w-full h-full object-cover"
              />
            </div>
            <!-- Decorative card -->
            <div class="absolute -bottom-6 -right-6 bg-paper border-2 border-accent/30 rounded-xl p-6 shadow-lg max-w-xs">
              <p class="text-3xl font-serif text-accent font-bold mb-2">{t.features.experienceBadge.years}</p>
              <p class="text-sm text-secondary">{t.features.experienceBadge.text}</p>
            </div>
          </div>
        </div>

        <!-- Right content -->
        <div class="scroll-animate order-1 lg:order-2">
          <div class="mb-8">
            <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
              {t.features.tag}
            </span>
            <h2 class="text-4xl font-serif font-bold text-ink mb-6">
              {t.features.title}
            </h2>
            <div class="w-20 h-1 bg-accent/50 mb-8"></div>
          </div>

          <div class="space-y-6">
            {features.map((feature) => (
              <div class="flex items-start space-x-4">
                <div class={`w-12 h-12 bg-${feature.color}/10 rounded-full flex items-center justify-center flex-shrink-0`}>
                  <span class={`text-xl text-${feature.color}`}>{feature.emoji}</span>
                </div>
                <div>
                  <h3 class="text-xl font-serif font-bold text-ink mb-2">
                    {t.features.items[feature.key].title}
                  </h3>
                  <p class="text-secondary">
                    {t.features.items[feature.key].desc}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Registration Form Area -->
  <section id="registration-form" class="py-24 scroll-mt-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
        <!-- Left: Course Benefits -->
        <div class="scroll-animate">
          <div class="mb-8">
            <span class="inline-block px-3 py-1 text-sm font-medium text-cyan bg-cyan/10 rounded-full mb-4">
              {t.registration.tag}
            </span>
            <h2 class="text-4xl font-serif font-bold text-ink mb-6">
              {t.registration.title}
            </h2>
            <div class="w-20 h-1 bg-cyan/50 mb-8"></div>
          </div>

          <div class="space-y-6 mb-8">
            <p class="text-secondary text-lg leading-relaxed">
              {t.registration.subtitle.split('24 hours')[0]}<span class="text-accent font-medium">{t.registration.hours}</span>{t.registration.subtitle.split('24 hours')[1]}
            </p>
          </div>

          <!-- Class Schedule -->
          <div class="bg-mist/20 rounded-xl p-6 mb-6">
            <h3 class="text-xl font-serif font-bold text-ink mb-4 flex items-center">
              <span class="mr-2">📅</span>
              {t.registration.schedule.title}
            </h3>
            <div class="space-y-3 text-secondary">
              <p class="text-accent font-medium mb-3">{t.registration.schedule.flexTitle}</p>
              <p>{t.registration.schedule.desc}</p>
            </div>
          </div>

          <!-- Course Pricing -->
          <div class="bg-mist/20 rounded-xl p-6 mb-6">
            <h3 class="text-xl font-serif font-bold text-ink mb-4 flex items-center">
              <span class="mr-2">💰</span>
              {t.registration.pricing.title}
            </h3>
            <div class="space-y-3 text-secondary">
              <p class="text-accent font-medium mb-3">{t.registration.pricing.flexTitle}</p>
              <p>{t.registration.pricing.desc}</p>
            </div>
          </div>

          <!-- Contact Information -->
          <div class="bg-mist/20 rounded-xl p-6">
            <h3 class="text-xl font-serif font-bold text-ink mb-4 flex items-center">
              <span class="mr-2">📧</span>
              {t.registration.contact.title}
            </h3>
            <div class="space-y-3 text-secondary">
              <div class="flex items-center space-x-3">
                <span class="text-accent">{t.registration.contact.emailLabel}</span>
                <a href={`mailto:${t.registration.contact.email}`} class="font-medium text-ink hover:text-accent transition-colors">{t.registration.contact.email}</a>
              </div>
            </div>
          </div>
        </div>

        <!-- Right: Registration Form -->
        <div class="scroll-animate" style="animation-delay: 0.2s">
          <RegistrationForm client:load locale={currentLocale} />
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-24 bg-mist/20">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
          {t.faq.tag}
        </span>
        <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink mb-6">
          {t.faq.title}
        </h2>
        <div class="w-32 h-1 bg-accent/50 mx-auto mb-6"></div>
      </div>

      <div class="space-y-6">
        {faqs.map((faq) => (
          <div class="scroll-animate" style={`animation-delay: ${faq.delay}`}>
            <div class="bg-paper rounded-xl shadow-lg p-8 border border-border/30">
              <h3 class="text-xl font-serif font-bold text-ink mb-4">
                {t.faq[faq.key].question}
              </h3>
              <p class="text-secondary leading-relaxed">
                {t.faq[faq.key].answer}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
      <h2 class="text-4xl font-serif font-bold text-ink mb-6">
        {t.cta.title}
      </h2>
      <p class="text-xl text-secondary mb-12">
        {t.cta.subtitle}
      </p>
      <a
        href="#registration-form"
        class="inline-block px-8 py-4 bg-accent hover:bg-primary text-paper hover:text-paper border-2 border-accent hover:border-primary rounded-lg font-medium text-lg transition-all duration-300 hover:scale-105 hover:shadow-xl"
      >
        {t.cta.button}
      </a>
    </div>
  </section>
</BaseLayout>

<script>
  // Scroll trigger animation
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1,
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.addEventListener('astro:page-load', () => {
    const animatedElements = document.querySelectorAll('.scroll-animate');
    animatedElements.forEach((el) => observer.observe(el));
  });

  const animatedElements = document.querySelectorAll('.scroll-animate');
  animatedElements.forEach((el) => observer.observe(el));
</script>

<style>
  .scroll-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .scroll-animate.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out forwards;
  }
</style>
