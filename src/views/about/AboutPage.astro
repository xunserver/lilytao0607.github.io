---
/**
 * AboutPage - 四川杨式太极拳专题页面组件
 * @param locale - 语言代码 ('nl' | 'en' | 'de' | 'zh')
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations } from '../../lib/i18n';
import { localizedPath } from '../../lib/paths';

interface Props {
  locale: 'nl' | 'en' | 'de' | 'zh';
}

const { locale } = Astro.props;
const currentLocale = locale;
const t = getTranslations('about', currentLocale);
const gt = getTranslations('global', currentLocale);

// 罗马数字映射
const romanNumerals = ['I', 'II', 'III', 'IV', 'V', 'VI', 'VII', 'VIII'];
---

<BaseLayout page="about" locale={currentLocale}>
  <!-- Hero Section -->
  <section class="relative min-h-[70vh] flex items-center justify-center overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-mist/50 via-paper to-mist/30"></div>
      <div class="absolute top-20 right-20 w-96 h-96 bg-accent/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 left-20 w-96 h-96 bg-cyan/5 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
      <div class="animate-fade-in">
        <span class="inline-block px-4 py-2 text-sm font-medium text-accent bg-accent/10 rounded-full mb-6">
          {t.hero.tag}
        </span>
        <h1 class="text-5xl md:text-7xl font-serif font-bold text-ink mb-6">
          {t.hero.title}
        </h1>
        <p class="text-xl text-secondary max-w-3xl mx-auto mb-4">
          {t.hero.subtitle}
        </p>
        <p class="text-lg text-secondary max-w-3xl mx-auto">
          {t.hero.explore}
        </p>
      </div>
    </div>
  </section>

  <!-- Chapter I: Origin - Chenjiagou -->
  <section class="py-24 relative overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-0 right-0 w-96 h-96 bg-accent/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-96 h-96 bg-cyan/5 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center scroll-animate">
        <!-- Chapter number -->
        <div class="mb-8">
          <span class="inline-block px-6 py-3 bg-accent/10 border-2 border-accent/30 rounded-full">
            <span class="text-4xl font-serif text-accent font-bold">{t.origin.chapter}</span>
          </span>
        </div>

        <!-- Tag -->
        <span class="inline-block px-4 py-2 text-sm font-medium text-cyan bg-cyan/10 rounded-full mb-6">
          {t.origin.tag}
        </span>

        <!-- Title -->
        <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink mb-8">
          {t.origin.title}
        </h2>

        <!-- Decorative line -->
        <div class="flex items-center justify-center gap-4 mb-12">
          <div class="w-20 h-0.5 bg-accent/30"></div>
          <div class="w-3 h-3 bg-accent/50 rotate-45"></div>
          <div class="w-20 h-0.5 bg-accent/30"></div>
        </div>

        <!-- Content -->
        <div class="bg-paper/80 backdrop-blur-sm rounded-2xl shadow-lg p-8 md:p-12 border border-border/30 mb-8">
          <div class="text-secondary text-lg leading-relaxed" set:html={t.origin.content} />
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap justify-center gap-4">
          {t.origin.tags.map((tag) => (
            <div class="px-6 py-3 bg-gradient-to-br from-accent/5 to-accent/10 border border-accent/20 rounded-xl text-ink text-sm font-medium">
              {tag}
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter II: Yang Style - Yang Luchan -->
  <section class="py-24 bg-gradient-to-br from-mist/50 via-paper to-mist/30">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="scroll-animate">
        <!-- Header -->
        <div class="text-center mb-12">
          <div class="flex items-center justify-center gap-6 mb-6">
            <span class="px-6 py-3 bg-accent/10 border-2 border-accent/30 rounded-full">
              <span class="text-3xl font-serif text-accent font-bold">{t.yangStyle.chapter}</span>
            </span>
          </div>
          <span class="inline-block px-4 py-2 text-sm font-medium text-accent bg-accent/10 rounded-full mb-6">
            {t.yangStyle.tag}
          </span>
          <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink">
            {t.yangStyle.title}
          </h2>
        </div>

        <!-- Main content -->
        <div class="bg-paper/90 backdrop-blur-sm rounded-2xl shadow-xl p-8 md:p-12 border border-border/30 mb-8">
          <div class="text-secondary text-lg leading-relaxed mb-8" set:html={t.yangStyle.content} />
        </div>

        <!-- Yang Style Characteristics -->
        <div class="bg-gradient-to-br from-accent/5 to-cyan/5 rounded-2xl shadow-lg p-8 md:p-10 border border-accent/20">
          <div class="text-center mb-8">
            <h3 class="text-2xl md:text-3xl font-serif font-bold text-ink mb-4">
              {t.yangStyle.characteristics.title}
            </h3>
            <div class="w-24 h-0.5 bg-accent/50 mx-auto"></div>
          </div>
          <p class="text-secondary text-center mb-8 max-w-2xl mx-auto">
            {t.yangStyle.characteristics.intro}
          </p>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {t.yangStyle.characteristics.items.map((item, index) => (
              <div class="flex items-start space-x-3 p-4 bg-paper/60 rounded-xl h-full">
                <span class="text-accent text-xl flex-shrink-0 mt-0.5">
                  {index === 0 ? '☯' : index === 1 ? '氣' : index === 2 ? '⚭' : index === 3 ? '◯' : '✓'}
                </span>
                <span class="text-secondary leading-relaxed flex-1">{item}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter III: Yang Chengfu -->
  <section class="py-24 relative overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-cyan/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-accent/5 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="scroll-animate">
        <!-- Header -->
        <div class="text-center mb-12">
          <span class="inline-block px-6 py-3 bg-cyan/10 border-2 border-cyan/30 rounded-full mb-6">
            <span class="text-3xl font-serif text-cyan font-bold">{t.yangChengfu.chapter}</span>
          </span>
          <span class="inline-block px-4 py-2 text-sm font-medium text-cyan bg-cyan/10 rounded-full mb-6">
            {t.yangChengfu.tag}
          </span>
          <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink">
            {t.yangChengfu.title}
          </h2>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
          <!-- Main content -->
          <div class="lg:col-span-2">
            <div class="bg-paper/90 backdrop-blur-sm rounded-2xl shadow-xl p-8 border border-border/30 h-full">
              <div class="text-secondary text-lg leading-relaxed" set:html={t.yangChengfu.content} />
            </div>
          </div>

          <!-- Historical contributions -->
          <div class="lg:col-span-1">
            <div class="bg-gradient-to-br from-cyan/5 to-accent/5 rounded-2xl shadow-lg p-8 border border-cyan/20 h-full">
              <div class="mb-6">
                <div class="w-12 h-12 bg-cyan/10 rounded-full flex items-center justify-center mx-auto mb-4">
                  <span class="text-2xl">📜</span>
                </div>
                <h3 class="text-xl font-serif font-bold text-ink text-center">
                  {t.yangChengfu.contributions.title}
                </h3>
                <div class="w-16 h-0.5 bg-cyan/50 mx-auto mt-3"></div>
              </div>
              <ul class="space-y-4">
                {t.yangChengfu.contributions.items.map((item, index) => (
                  <li class="flex items-start space-x-3">
                    <span class="flex-shrink-0 w-7 h-7 bg-cyan/10 rounded-full flex items-center justify-center text-cyan text-sm font-bold">
                      {index + 1}
                    </span>
                    <span class="text-secondary leading-relaxed">{item}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter IV: Li Yaxuan - Founder of Sichuan Yang-Style -->
  <section class="py-20 bg-mist/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
          {t.liYaxuan.tag}
        </span>
        <div class="flex items-center justify-center gap-4 mb-4">
          <span class="text-4xl font-serif text-accent font-bold">{t.liYaxuan.chapter}</span>
          <h2 class="text-3xl md:text-4xl font-serif font-bold text-ink">
            {t.liYaxuan.title}
          </h2>
        </div>
        <p class="text-xl text-secondary max-w-3xl mx-auto mb-6">
          {t.liYaxuan.subtitle}
        </p>
        <p class="text-lg text-secondary max-w-4xl mx-auto leading-relaxed">
          {t.liYaxuan.intro}
        </p>
      </div>

      <!-- Master Image -->
      <div class="mb-12 scroll-animate">
        <div class="max-w-md mx-auto">
          <div class="aspect-[4/5] rounded-2xl overflow-hidden shadow-2xl border-4 border-accent/20">
            <img
              src="/image/tai-chi-liyaxuan.jpg"
              alt="Li Yaxuan"
              class="w-full h-full object-cover"
            />
          </div>
          <p class="text-center text-sm text-secondary mt-3">李雅轩 Li Yaxuan (1894-1976)</p>
        </div>
      </div>

      <!-- Teaching & Disciples Section -->
      <div class="scroll-animate mb-12">
        <div class="bg-gradient-to-br from-accent/5 to-cyan/5 rounded-2xl shadow-lg p-8 md:p-10 border border-accent/20">
          <div class="text-center mb-8">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-accent/10 rounded-full mb-4">
              <span class="text-3xl">👨‍👩‍👧‍👦</span>
            </div>
            <h3 class="text-2xl md:text-3xl font-serif font-bold text-ink mb-4">
              {t.liYaxuan.disciples.title}
            </h3>
            <div class="w-24 h-0.5 bg-accent/50 mx-auto mb-6"></div>
          </div>

          <p class="text-secondary text-lg leading-relaxed text-center max-w-3xl mx-auto mb-8">
            {t.liYaxuan.disciples.content}
          </p>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {t.liYaxuan.disciples.highlights.map((item) => (
              <div class="flex items-center space-x-3 p-4 bg-paper/60 rounded-xl h-full">
                <span class="text-accent text-2xl flex-shrink-0">✨</span>
                <span class="text-secondary leading-relaxed flex-1">{item}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-12">
        <!-- Left: Heritage lineage -->
        <div class="scroll-animate">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30">
            <div class="mb-6">
              <h3 class="text-2xl font-serif font-bold text-ink mb-3">
                {t.liYaxuan.heritageTitle}
              </h3>
              <div class="w-16 h-1 bg-accent/50"></div>
            </div>
            <p class="text-secondary leading-relaxed" set:html={t.liYaxuan.heritageContent} />
          </div>
        </div>

        <!-- Right: Position in Sichuan -->
        <div class="scroll-animate" style="animation-delay: 0.2s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30">
            <div class="mb-6">
              <h3 class="text-2xl font-serif font-bold text-ink mb-3">
                {t.liYaxuan.positionTitle}
              </h3>
              <div class="w-16 h-1 bg-cyan/50"></div>
            </div>
            <p class="text-secondary leading-relaxed" set:html={t.liYaxuan.positionContent} />
          </div>
        </div>
      </div>

      <!-- Sichuan Yang-Style Characteristics -->
      <div class="scroll-animate">
        <div class="bg-paper/80 rounded-2xl shadow-lg p-8 border border-accent/20">
          <h3 class="text-2xl font-serif font-bold text-ink mb-6 text-center">
            {t.liYaxuan.characteristics.title}
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {t.liYaxuan.characteristics.items.map((item) => (
              <div class="flex items-start space-x-3 h-full">
                <span class="text-accent text-2xl flex-shrink-0">✓</span>
                <span class="text-secondary leading-relaxed flex-1">{item}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter V: Lin Mogen - The Great Spreader -->
  <section class="py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <!-- Left: Content -->
        <div class="scroll-animate order-2 lg:order-1">
          <div class="mb-6">
            <span class="inline-block px-3 py-1 text-sm font-medium text-cyan bg-cyan/10 rounded-full mb-4">
              {t.linMogen.tag}
            </span>
            <div class="flex items-center gap-4 mb-4">
              <span class="text-3xl font-serif text-cyan font-bold">{t.linMogen.chapter}</span>
              <h2 class="text-3xl md:text-4xl font-serif font-bold text-ink">
                {t.linMogen.title}
              </h2>
            </div>
            <p class="text-lg text-accent mb-6">{t.linMogen.subtitle}</p>
            <div class="w-20 h-1 bg-cyan/50"></div>
          </div>

          <div class="text-secondary text-lg leading-relaxed mb-6" set:html={t.linMogen.content} />

          <!-- Historical positioning -->
          <div class="bg-cyan/5 border border-cyan/20 rounded-xl p-6 mb-6">
            <h3 class="text-xl font-serif font-bold text-ink mb-3">
              {t.linMogen.positioning.title}
            </h3>
            <p class="text-secondary">{t.linMogen.positioning.content}</p>
          </div>

          <!-- His influence -->
          <div class="bg-accent/5 border border-accent/20 rounded-xl p-6">
            <h3 class="text-xl font-serif font-bold text-ink mb-4">
              {t.linMogen.contributions.title}
            </h3>
            <ul class="space-y-2 text-secondary">
              {t.linMogen.contributions.items.map((item) => (
                <li class="flex items-start space-x-2">
                  <span class="text-accent mt-1">•</span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <!-- Right: Image -->
        <div class="scroll-animate order-1 lg:order-2">
          <div class="relative">
            <div class="aspect-[4/5] rounded-2xl overflow-hidden shadow-2xl border-4 border-cyan/20">
              <img
                src="/image/tai-chi-linmogen.jpg"
                alt="Lin Mogen"
                class="w-full h-full object-cover"
              />
            </div>
            <p class="text-center text-sm text-secondary mt-3">林墨根 Lin Mogen</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter VI: He Shaojun - Contemporary Master -->
  <section class="py-20 bg-mist/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
          {t.heShaojun.tag}
        </span>
        <div class="flex items-center justify-center gap-4 mb-4">
          <span class="text-4xl font-serif text-accent font-bold">{t.heShaojun.chapter}</span>
          <h2 class="text-3xl md:text-4xl font-serif font-bold text-ink">
            {t.heShaojun.title}
          </h2>
        </div>
      </div>

      <!-- Master Image -->
      <div class="mb-12 scroll-animate">
        <div class="max-w-md mx-auto">
          <div class="aspect-[4/5] rounded-2xl overflow-hidden shadow-2xl border-4 border-accent/20">
            <img
              src="/image/tai-chi-heshaojun.jpg"
              alt="He Shaojun"
              class="w-full h-full object-cover"
            />
          </div>
          <p class="text-center text-sm text-secondary mt-3">何绍君 He Shaojun</p>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Training background -->
        <div class="scroll-animate h-full flex flex-col">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="w-12 h-12 bg-accent/10 rounded-full flex items-center justify-center mb-4">
              <span class="text-2xl">🎓</span>
            </div>
            <h3 class="text-xl font-serif font-bold text-ink mb-3">
              {t.heShaojun.background.title}
            </h3>
            <p class="text-secondary leading-relaxed flex-1">
              {t.heShaojun.background.content}
            </p>
          </div>
        </div>

        <!-- Innovation contribution -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.1s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="w-12 h-12 bg-cyan/10 rounded-full flex items-center justify-center mb-4">
              <span class="text-2xl">💡</span>
            </div>
            <h3 class="text-xl font-serif font-bold text-ink mb-3">
              {t.heShaojun.innovation.title}
            </h3>
            <p class="text-secondary leading-relaxed flex-1">
              {t.heShaojun.innovation.content}
            </p>
          </div>
        </div>

        <!-- His contribution -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.2s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="w-12 h-12 bg-secondary/10 rounded-full flex items-center justify-center mb-4">
              <span class="text-2xl">🥋</span>
            </div>
            <h3 class="text-xl font-serif font-bold text-ink mb-3">
              {t.heShaojun.contribution.title}
            </h3>
            <p class="text-secondary leading-relaxed flex-1">
              {t.heShaojun.contribution.content}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter VII: Tao Li - International Ambassador -->
  <section class="py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-cyan bg-cyan/10 rounded-full mb-4">
          {t.taoLi.tag}
        </span>
        <div class="flex items-center justify-center gap-4 mb-4">
          <span class="text-4xl font-serif text-cyan font-bold">{t.taoLi.chapter}</span>
          <h2 class="text-3xl md:text-4xl font-serif font-bold text-ink">
            {t.taoLi.title}
          </h2>
        </div>
      </div>

      <!-- Master Image -->
      <div class="mb-12 scroll-animate">
        <div class="max-w-md mx-auto">
          <div class="aspect-[4/5] rounded-2xl overflow-hidden shadow-2xl border-4 border-cyan/20">
            <img
              src="/image/taichi-baishi.jpeg"
              alt="Tao Li Bai Shi Ceremony"
              class="w-full h-full object-cover"
            />
          </div>
          <p class="text-center text-sm text-secondary mt-3">陶莉拜师 Tao Li Bai Shi Ceremony</p>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
        <!-- Left: Background -->
        <div class="scroll-animate h-full flex flex-col">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <h3 class="text-2xl font-serif font-bold text-ink mb-3">
                {t.taoLi.background.title}
              </h3>
              <div class="w-16 h-1 bg-accent/50"></div>
            </div>
            <p class="text-secondary leading-relaxed flex-1">
              {t.taoLi.background.content}
            </p>
          </div>
        </div>

        <!-- Right: Contributions -->
        <div class="scroll-animate h-full flex flex-col" style="animation-delay: 0.2s">
          <div class="bg-paper rounded-2xl shadow-lg p-8 border border-border/30 flex-1 flex flex-col">
            <div class="mb-6">
              <h3 class="text-2xl font-serif font-bold text-ink mb-3">
                {t.taoLi.contributions.title}
              </h3>
              <div class="w-16 h-1 bg-cyan/50"></div>
            </div>
            <ul class="space-y-3 text-secondary mb-6 flex-1">
              {t.taoLi.contributions.items.map((item) => (
                <li class="flex items-start space-x-2">
                  <span class="text-cyan mt-1">✓</span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
            <p class="text-accent font-medium mt-auto">
              {t.taoLi.conclusion}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Chapter VIII: Lineage Summary -->
  <section class="py-20 bg-mist/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 scroll-animate">
        <span class="inline-block px-3 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
          {t.lineage.tag}
        </span>
        <div class="flex items-center justify-center gap-4 mb-4">
          <span class="text-4xl font-serif text-accent font-bold">{t.lineage.chapter}</span>
          <h2 class="text-3xl md:text-4xl font-serif font-bold text-ink">
            {t.lineage.title}
          </h2>
        </div>
        <p class="text-lg text-secondary">
          {t.lineage.subtitle}
        </p>
      </div>

      <!-- Timeline -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {t.lineage.stages.map((stage, index) => (
          <div class="scroll-animate h-full flex flex-col" style={`animation-delay: ${index * 0.1}s`}>
            <div class="bg-paper rounded-2xl shadow-lg p-6 border border-border/30 flex-1 flex flex-col">
              <div class="w-10 h-10 bg-accent/10 rounded-full flex items-center justify-center mb-4">
                <span class="text-lg font-serif text-accent font-bold">{index + 1}</span>
              </div>
              <h3 class="text-xl font-serif font-bold text-ink mb-3">
                {stage.title}
              </h3>
              <p class="text-secondary leading-relaxed flex-1">
                {stage.content}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
      <h2 class="text-4xl font-serif font-bold text-ink mb-6">
        {t.cta.title}
      </h2>
      <p class="text-xl text-secondary mb-12">
        {t.cta.subtitle}
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-6">
        <a
          href={localizedPath('/me', currentLocale)}
          class="px-8 py-4 bg-accent hover:bg-primary text-paper hover:text-paper border-2 border-accent hover:border-primary rounded-lg font-medium text-lg transition-all duration-300 hover:scale-105 hover:shadow-xl"
        >
          {t.cta.meButton}
        </a>
        <a
          href={localizedPath('/courses', currentLocale)}
          class="px-8 py-4 border-2 border-accent/30 text-accent hover:bg-accent/10 rounded-lg font-medium text-lg transition-all duration-300 hover:scale-105"
        >
          {t.cta.coursesButton}
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  // Scroll-triggered animation
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1,
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.addEventListener('astro:page-load', () => {
    const animatedElements = document.querySelectorAll('.scroll-animate');
    animatedElements.forEach((el) => observer.observe(el));
  });

  const animatedElements = document.querySelectorAll('.scroll-animate');
  animatedElements.forEach((el) => observer.observe(el));
</script>

<style>
  .scroll-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .scroll-animate.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out forwards;
  }
</style>
