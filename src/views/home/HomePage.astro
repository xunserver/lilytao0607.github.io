---
/**
 * HomePage - 共享的首页组件
 * @param locale - 语言代码 ('nl' | 'en' | 'de' | 'pl' | 'zh')
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations } from '../../lib/i18n';
import { localizedPath } from '../../lib/paths';

interface Props {
  locale: 'nl' | 'en' | 'de' | 'pl' | 'zh';
}

const { locale } = Astro.props;
const currentLocale = locale;
const t = getTranslations('home', currentLocale);
const gt = getTranslations('global', currentLocale); // global translations (nav, etc)
const coursesT = getTranslations('courses', currentLocale); // courses page translations

// Quote cards data - structure is shared, translations from i18n
const quoteCards = [
  { key: 'water', color: 'accent' },
  { key: 'yi', color: 'cyan' },
  { key: 'zen', color: 'secondary' },
  { key: 'tao', color: 'accent' },
  { key: 'still', color: 'cyan' },
  { key: 'wisdom', color: 'secondary' },
];
---

<BaseLayout page="home" locale={currentLocale}>
  <!-- Hero Section -->
  <section class="relative min-h-screen flex flex-col items-center justify-center overflow-hidden pt-20 pb-12">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-gradient-to-br from-paper via-mist/30 to-paper"></div>
      <div class="absolute inset-0 bg-gradient-to-b from-paper/90 via-paper/70 to-paper/95"></div>
      <div class="absolute top-0 left-0 right-0 h-1/2 bg-gradient-to-b from-accent/5 to-transparent"></div>
      <div class="absolute bottom-0 left-0 right-0 h-1/3 bg-gradient-to-t from-secondary/10 to-transparent"></div>
      <div class="absolute top-20 left-10 w-64 h-64 bg-accent/5 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 right-10 w-96 h-96 bg-cyan/5 rounded-full blur-3xl"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-secondary/3 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col items-center">
      <div class="relative mb-12 animate-fade-in">
        <div class="absolute inset-0 -z-10 flex items-center justify-center">
          <div class="decorative-ring ring-outer w-[120%] h-[120%] rounded-full border border-accent/20"></div>
          <div class="absolute decorative-ring ring-inner w-[140%] h-[140%] rounded-full border border-cyan/10"></div>
        </div>

        <img
          src="/image/taichi-logo.png"
          alt="Tai Chi"
          width={400}
          height={400}
          class="w-64 h-64 md:w-80 md:h-80 object-contain drop-shadow-2xl taiji-float-spin"
        />
      </div>

      <div class="text-center space-y-6 animate-fade-in" style="animation-delay: 0.3s">
        <h1 class="text-4xl md:text-5xl font-serif font-bold text-ink leading-relaxed">
          {t.hero.zenQuote}
        </h1>
        <p class="text-xl md:text-2xl text-secondary font-light leading-relaxed max-w-3xl mx-auto">
          {t.hero.zenSub}
          {t.hero.zenSubExtra && (<><br class="hidden sm:block" />{t.hero.zenSubExtra}</>)}
        </p>
        <div class="w-24 h-0.5 bg-accent/50 mx-auto mt-8"></div>
      </div>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mt-12 animate-fade-in" style="animation-delay: 0.6s">
        <a
          href={localizedPath('/movements', currentLocale)}
          class="group px-8 py-3 bg-accent hover:bg-primary text-paper rounded-lg font-medium transition-all duration-300 hover:scale-105 inline-flex items-center"
        >
          {t.hero.explore}
          <span class="inline-block ml-2 transition-transform group-hover:translate-x-1">→</span>
        </a>
        <a
          href={localizedPath('/about', currentLocale)}
          class="px-8 py-3 border-2 border-accent/30 text-accent hover:bg-accent/10 rounded-lg font-medium transition-all duration-300 hover:scale-105"
        >
          {gt.nav.about}
        </a>
      </div>
    </div>

    <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-paper to-transparent"></div>
  </section>

  <!-- Quote Cards Section -->
  <section class="py-24 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-paper to-mist/20"></div>
      <div class="absolute top-1/4 right-0 w-96 h-96 bg-accent/3 rounded-full blur-3xl"></div>
      <div class="absolute bottom-1/4 left-0 w-96 h-96 bg-cyan/3 rounded-full blur-3xl"></div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16 scroll-animate">
        <div class="inline-block">
          <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink mb-4">
            {t.quotes.title}
          </h2>
          <div class="w-32 h-1 bg-accent/50 mx-auto mt-4"></div>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {quoteCards.map((card, index) => (
          <div class="scroll-animate group relative h-full flex flex-col" style={`animation-delay: ${index * 0.15}s`}>
            <div class="relative bg-paper rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-500 p-8 border border-border/30 hover:border-accent/50 overflow-hidden flex-1 flex flex-col">
              <div class="absolute top-6 right-6 w-16 h-16 bg-accent/10 rounded-full flex items-center justify-center group-hover:bg-accent/20 transition-colors duration-300">
                <span class="text-accent text-2xl font-serif">{t.quotes[card.key].char}</span>
              </div>

              <div class="text-6xl text-accent/20 font-serif leading-none mb-4">"</div>

              <blockquote class="relative z-10 flex-1 flex flex-col">
                <p class="text-xl md:text-2xl font-serif text-ink leading-relaxed mb-6 flex-1">
                  {t.quotes[card.key].quote}
                </p>
                <footer class="flex items-center space-x-3 mt-auto">
                  <div class="w-12 h-0.5 bg-accent/50"></div>
                  <cite class="text-secondary not-italic font-medium">— {t.quotes[card.key].source}</cite>
                </footer>
              </blockquote>

              <div class="absolute bottom-0 right-0 w-32 h-32 bg-accent/5 rounded-full blur-2xl group-hover:scale-150 transition-transform duration-500"></div>
            </div>
          </div>
        ))}
      </div>

      <div class="mt-16 flex justify-center items-center space-x-4 scroll-animate" style="animation-delay: 0.6s">
        <div class="w-24 h-px bg-gradient-to-r from-transparent to-accent/30"></div>
        <div class="w-2 h-2 rounded-full bg-accent/50"></div>
        <div class="w-24 h-px bg-gradient-to-l from-transparent to-accent/30"></div>
      </div>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section class="py-24 bg-mist/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <div class="scroll-animate order-2 lg:order-1">
          <div class="relative">
            <div class="relative rounded-2xl overflow-hidden shadow-2xl border border-border/30">
              <div class="aspect-[4/3] bg-gradient-to-br from-secondary/20 to-accent/20">
                <div class="w-full h-full flex items-center justify-center p-12">
                  <div class="relative">
                    <div class="absolute inset-0 -m-8 flex items-center justify-center">
                      <div class="w-80 h-80 rounded-full border-2 border-accent/30"></div>
                    </div>
                    <div class="absolute inset-0 -m-4 flex items-center justify-center">
                      <div class="w-64 h-64 rounded-full border border-cyan/20"></div>
                    </div>

                    <div class="relative">
                      <img
                        src="/image/taichi-logo.png"
                        alt="Tai Chi"
                        width={300}
                        height={300}
                        class="w-56 h-56 object-contain drop-shadow-xl taiji-spin-slow"
                      />
                    </div>
                  </div>
                </div>
              </div>

              <div class="absolute -bottom-4 -right-4 w-32 h-32 bg-accent/10 rounded-full blur-2xl"></div>
              <div class="absolute -top-4 -left-4 w-24 h-24 bg-cyan/10 rounded-full blur-2xl"></div>
            </div>

            <div class="absolute -bottom-6 -left-6 lg:left-0 bg-paper rounded-xl shadow-lg p-6 border border-border/50 max-w-xs">
              <div class="flex items-center space-x-4">
                <div class="w-12 h-12 bg-accent/10 rounded-full flex items-center justify-center flex-shrink-0">
                  <span class="text-accent text-xl font-serif">阴</span>
                </div>
                <div>
                  <p class="text-sm text-secondary">Yin Yang Harmony</p>
                  <p class="text-lg font-serif font-bold text-ink">Balance</p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="scroll-animate order-1 lg:order-2">
          <div class="mb-8">
            <span class="inline-block px-4 py-1 text-sm font-medium text-accent bg-accent/10 rounded-full mb-4">
              {t.philosophy.tag}
            </span>
            <h2 class="text-4xl md:text-5xl font-serif font-bold text-ink mb-6 leading-tight">
              {t.philosophy.title}
            </h2>
            <div class="w-20 h-1 bg-accent/50 mb-8"></div>
          </div>

          <div class="space-y-6 text-secondary">
            <p class="text-lg leading-relaxed">
              {t.philosophy.p1}
            </p>
            <p class="text-lg leading-relaxed">
              {t.philosophy.p2}
            </p>
          </div>

          <div class="mt-8 flex flex-wrap gap-3">
            <span class="px-4 py-2 bg-paper border border-border/50 rounded-lg text-sm text-ink">
              🧘 {t.philosophy.tag1}
            </span>
            <span class="px-4 py-2 bg-paper border border-border/50 rounded-lg text-sm text-ink">
              💪 {t.philosophy.tag2}
            </span>
            <span class="px-4 py-2 bg-paper border border-border/50 rounded-lg text-sm text-ink">
              ☯️ {t.philosophy.tag3}
            </span>
          </div>

          <a
            href={localizedPath('/me', currentLocale)}
            class="inline-flex items-center mt-8 px-8 py-3 bg-accent hover:bg-primary text-paper rounded-lg font-medium transition-all duration-300 hover:scale-105"
          >
            {t.philosophy.learnMore}
            <span class="ml-2">→</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="py-20 bg-mist/30">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16 scroll-animate">
        <h2 class="text-4xl font-serif font-bold text-ink mb-4">
          {t.benefits.title}
        </h2>
        <p class="text-lg text-secondary">
          {t.benefits.subtitle}
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="scroll-animate h-full flex flex-col bg-paper rounded-xl shadow-lg p-8 hover:shadow-xl transition-shadow duration-300 border border-border/50">
          <div class="w-16 h-16 bg-accent/10 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-serif font-bold text-ink mb-3">
            {t.benefits.card1.title}
          </h3>
          <p class="text-secondary flex-1">
            {t.benefits.card1.desc}
          </p>
        </div>

        <div class="scroll-animate h-full flex flex-col bg-paper rounded-xl shadow-lg p-8 hover:shadow-xl transition-shadow duration-300 border border-border/50" style="animation-delay: 0.2s">
          <div class="w-16 h-16 bg-accent/10 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>
          <h3 class="text-xl font-serif font-bold text-ink mb-3">
            {t.benefits.card2.title}
          </h3>
          <p class="text-secondary flex-1">
            {t.benefits.card2.desc}
          </p>
        </div>

        <div class="scroll-animate h-full flex flex-col bg-paper rounded-xl shadow-lg p-8 hover:shadow-xl transition-shadow duration-300 border border-border/50" style="animation-delay: 0.4s">
          <div class="w-16 h-16 bg-accent/10 rounded-lg flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-serif font-bold text-ink mb-3">
            {t.benefits.card3.title}
          </h3>
          <p class="text-secondary flex-1">
            {t.benefits.card3.desc}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
      <h2 class="text-4xl font-serif font-bold text-ink mb-6">
        {t.cta.title}
      </h2>
      <p class="text-xl text-secondary mb-8">
        {t.cta.subtitle}
      </p>
      <a
        href={localizedPath('/courses', currentLocale)}
        class="inline-block px-10 py-4 bg-accent hover:bg-primary text-paper rounded-lg font-medium text-lg transition-all duration-300 hover:scale-105 hover:shadow-xl"
      >
        {coursesT.register}
      </a>
    </div>
  </section>
</BaseLayout>

<script>
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1,
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  function observeAnimations() {
    const animatedElements = document.querySelectorAll('.scroll-animate');
    animatedElements.forEach((el) => observer.observe(el));
  }

  // 首次加载时立即执行
  observeAnimations();

  // 客户端导航时重新执行
  document.addEventListener('astro:page-load', observeAnimations);
</script>

<style>
  .scroll-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .scroll-animate.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out forwards;
  }

  /* 装饰圆环旋转动画 */
  @keyframes spin-outer {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes spin-inner-reverse {
    from {
      transform: rotate(360deg);
    }
    to {
      transform: rotate(0deg);
    }
  }

  .decorative-ring.ring-outer {
    animation: spin-outer 60s linear infinite;
  }

  .decorative-ring.ring-inner {
    animation: spin-inner-reverse 80s linear infinite;
  }

  /* 太极慢速旋转动画 */
  @keyframes taiji-spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .taiji-spin-slow {
    animation: taiji-spin-slow 30s linear infinite;
  }

  /* 太极同时浮动和旋转的动画 */
  @keyframes taiji-float-spin {
    0% {
      transform: translateY(0px) rotate(0deg);
    }
    25% {
      transform: translateY(-20px) rotate(90deg);
    }
    50% {
      transform: translateY(0px) rotate(180deg);
    }
    75% {
      transform: translateY(-20px) rotate(270deg);
    }
    100% {
      transform: translateY(0px) rotate(360deg);
    }
  }

  .taiji-float-spin {
    animation: taiji-float-spin 12s ease-in-out infinite;
  }
</style>
