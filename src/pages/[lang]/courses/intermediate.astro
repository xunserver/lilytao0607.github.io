---
import IntermediateOutlinePage from '../../../views/courses/IntermediateOutlinePage.astro';
import { getLocalePaths, type Locale } from '@/lib/locales';

export const getStaticPaths = getLocalePaths;

const { lang } = Astro.params;
const locale = lang as Locale;
---
<IntermediateOutlinePage locale={locale} />
