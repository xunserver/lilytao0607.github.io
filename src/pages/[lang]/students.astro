---
/**
 * Students Page Route - 学员风采路由
 * Displays student stories and experiences
 */
import StudentsPage from '../../views/students/StudentsPage.astro';
import { getLocalePaths, type Locale } from '@/lib/locales';

export const getStaticPaths = getLocalePaths;

const { lang } = Astro.params;
const locale = lang as Locale;
---
<StudentsPage page="students" locale={locale} />
